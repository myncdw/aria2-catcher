# 产品需求文档（最终版）：Firefox 下载截获至 Aria2 RPC

---

## 文档信息

| 项目         | 内容                                               |
| ------------ | -------------------------------------------------- |
| **文档名称** | Firefox 扩展 - 下载截获至 Aria2 RPC 需求规格说明书 |
| **版本**     | 最终版                                           |
| **发布日期** | 2026-08-12                                         |
| **状态**     | ✅ 所有待确认项已闭环                               |

---

## 一、需求背景

### 1.1 为什么要做这个功能？

Firefox 原生下载管理器功能基础，缺乏对高级下载工具（如 Aria2）的原生集成支持。Aria2 具备多线程、断点续传、高速下载等优势，用户期望将 Firefox 的下载请求无缝转发至 Aria2 RPC 服务，同时保留浏览器原生下载作为备选方案。

### 1.2 解决的核心问题

| 序号 | 问题                                 | 解决方案                                     |
| ---- | ------------------------------------ | -------------------------------------------- |
| 1    | 每次下载需手动复制链接到 Aria2       | 自动截获 Firefox 下载请求，一键转发          |
| 2    | 下载链接需要鉴权（referrer/cookies） | 完整透传当前页面的 Headers 信息              |
| 3    | 不同场景需要不同的下载器             | 支持双下载器独立配置，灵活切换               |
| 4    | 下载方式单一                         | 提供 Aria2（双通道）和浏览器原生下载三种选项 |
| 5    | 界面体验不统一                       | 全 UI（弹窗/面板/设置页）支持深色模式        |

---

## 二、用户角色

| 角色                | 说明                          | 典型场景                                              |
| ------------------- | ----------------------------- | ----------------------------------------------------- |
| **普通用户**        | 使用浏览器下载文件的终端用户  | 点击下载链接 → 在弹窗中选择下载方式 → 文件开始下载    |
| **高级用户/管理员** | 需要配置 Aria2 连接参数的用户 | 进入设置页 → 配置 RPC 地址/密钥/名称 → 自定义下载行为 |

---

## 三、核心功能点

| 编号 | 功能                 | 一句话描述                                                   |
| ---- | -------------------- | ------------------------------------------------------------ |
| F-01 | **下载请求自动截获** | 全局开关开启时，拦截所有 HTTP/HTTPS 下载请求，替换 Firefox 原生下载窗口 |
| F-02 | **下载方式选择弹窗** | 弹出窗口展示文件名（可编辑）和下载链接，提供“下载器1/下载器2/保存”三种选项 |
| F-03 | **Headers 透传**     | 将当前页面的 `Referer`、`Cookies`、`User-Agent` 完整传递给 Aria2 RPC |
| F-04 | **双下载器独立配置** | 在设置页中分别为两个下载器配置名称、RPC 地址（协议/IP/端口/路径）和密钥 |
| F-05 | **扩展图标控制面板** | 点击工具栏图标弹出面板，包含全局开关和设置入口               |
| F-06 | **深色模式**         | 全 UI（弹窗/面板/设置页）支持深色模式，默认为深色，手动切换  |
| F-07 | **下载完成通知**     | 使用 Windows 系统通知，在 Aria2 下载完成时可选提示           |
| F-08 | **弹窗方式切换**     | 支持“浏览器新窗口”和“页面内右上角覆盖弹出”两种弹窗形式       |

---

## 四、详细业务规则

### 4.1 下载截获规则

| 编号     | 规则描述                                                     | 优先级 |
| -------- | ------------------------------------------------------------ | ------ |
| **D-01** | 扩展全局开关 **开启** 时，所有由 Firefox 触发的 HTTP/HTTPS 下载请求均被截获，不弹出 Firefox 原生下载窗口 | P0     |
| **D-02** | 截获后立即弹出“下载方式选择弹窗”，此时 **尚未开始** 任何实际下载 | P0     |
| **D-03** | 扩展全局开关 **关闭** 时，完全绕过扩展逻辑，使用 Firefox 默认下载行为 | P0     |
| **D-04** | 截获范围排除：扩展自身的设置页面、扩展图标面板的请求，防止递归误拦截 | P1     |
| **D-05** | 对于 `blob:`、`data:`、`ftp:` 等非 HTTP/HTTPS 协议的下载链接，**全部直接交由 Firefox 浏览器处理**，不弹出扩展下载弹窗 | P0     |

---

### 4.2 下载方式选择弹窗规则

#### 4.2.1 弹窗形式

| 编号      | 规则描述                                                     | 优先级 |
| --------- | ------------------------------------------------------------ | ------ |
| **PW-01** | 弹窗提供两种形式，通过设置页“下载弹窗方式”切换：<br>① **浏览器窗口**（默认）：通过 `chrome.windows.create` 新建独立弹窗，无地址栏/书签栏，宽度 500px，高度自适应<br>② **页面内覆盖弹出**：在当前页面的右上角以浮动层（overlay）形式覆盖显示 | P0     |
| **PW-02** | 两种弹窗形式的交互逻辑完全一致（展示内容、按钮行为、取消方式等），仅呈现载体不同 | P1     |

#### 4.2.2 弹窗内容

```
┌─────────────────────────────────────────────────┐
│  [×]                                            │
│  文件名: [input: 可编辑]                        │
│  链接:   https://example.com/file.zip           │
│                                                  │
│  [ 下载器1 ]  [ 下载器2 ]  [ 保存 ]             │
└─────────────────────────────────────────────────┘
```

| 编号     | 规则描述                                                     | 优先级 |
| -------- | ------------------------------------------------------------ | ------ |
| **P-01** | 第一行展示 **文件名**，允许用户编辑修改，修改后传递给 Aria2 的 `out` 参数 | P0     |
| **P-02** | 第二行展示 **下载链接（URL）**，只读不可编辑；URL 过长时截断显示，hover 展示完整内容，并提供复制按钮 | P1     |
| **P-03** | 底部三个按钮从左至右：**下载器1**、**下载器2**、**保存**（浏览器下载） | P0     |
| **P-04** | “下载器1/2”按钮名称读取设置中的自定义名称，默认回退为“下载器1”/“下载器2” | P0     |
| **P-05** | **只有**用户点击三个按钮之一时，才开始执行对应的下载动作     | P0     |
| **P-06** | 用户关闭弹窗（点击右上角 × / 按 ESC / 点击弹窗外空白区域），**不进行任何下载操作，视为取消下载** | P0     |
| **P-07** | 若某一下载器未配置完整（RPC 地址为空或无效），对应按钮 **置灰禁用**，hover 提示“请先在设置中配置下载器1” | P0     |
| **P-08** | 点击任一按钮后，弹窗立即关闭，随后开始执行对应的下载动作     | P0     |

---

### 4.3 Aria2 RPC 调用规则

| 编号     | 规则描述                                                     | 优先级 |
| -------- | ------------------------------------------------------------ | ------ |
| **R-01** | 调用 Aria2 JSON-RPC 的 `aria2.addUri` 方法                   | P0     |
| **R-02** | 透传的 Headers 参数：`Referer`（当前页面 URL）、`Cookie`（当前页面上下文 Cookies）、`User-Agent`（当前浏览器 UA） | P0     |
| **R-03** | 调用时携带下载器配置中的 **密钥**（如有）进行 RPC 身份验证   | P0     |
| **R-04** | 下载文件名通过 `out` 参数传递，值为用户在弹窗中修改后的文件名 | P0     |
| **R-05** | **所有下载配置均使用 Aria2 服务端自身的配置**（如 `dir` 保存目录、并发数、分片大小等），扩展 **不做任何覆盖**。扩展仅传递：`url`、`out`、`referer`、`cookie`、`user-agent` | P0     |
| **R-06** | RPC 调用异常（网络超时、服务不可达、连接拒绝、返回错误码）时：<br>① 弹窗提示用户 **“下载失败”** <br>② 提供 **折叠/展开的错误详情**（默认折叠）<br>③ **取消本次下载**（不重试，不降级到浏览器保存） | P0     |
| **R-07** | 多文件并发下载由 Aria2 自身调度，扩展不实现队列管理          | P2     |

---

### 4.4 扩展图标面板规则

```
┌──────────────────────┐
│  ⚡ 下载截获扩展      │
│  状态: ● 已启用拦截   │
│  [═══════○══════]    │  ← 全局开关
│  [ ⚙ 设置 ]          │
└──────────────────────┘
```

| 编号     | 规则描述                                                     | 优先级 |
| -------- | ------------------------------------------------------------ | ------ |
| **I-01** | 点击浏览器工具栏中的扩展图标，弹出浮动面板（Popup），非模态，点击外部自动关闭 | P0     |
| **I-02** | 面板中包含 **全局开关**（Toggle 切换按钮），“开”=启用截获，“关”=使用 Firefox 默认下载，状态持久化保存 | P0     |
| **I-03** | 面板中包含 **设置按钮**，点击后在新标签页打开扩展的设置页面  | P0     |
| **I-04** | 面板中显示当前扩展状态提示（如“已启用拦截”/“已禁用”），便于用户确认 | P1     |

---

### 4.5 设置页面规则

#### 4.5.1 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  ⚙ 下载截获扩展 - 设置                                        │
│                                                                 │
│  ─── 下载器1 ─────────────────────────────────────────────────  │
│  名称: [下载器1              ]                                 │
│  地址: [HTTP▼] [192.168.1.10] : [6800] / [jsonrpc]            │
│  密钥: [****************    ] (可选)                           │
│                                                                 │
│  ─── 下载器2 ─────────────────────────────────────────────────  │
│  名称: [下载器2              ]                                 │
│  地址: [HTTPS▼] [aria2.example.com] : [443] / [jsonrpc]       │
│  密钥: [                     ] (可选)                          │
│                                                                 │
│  ─── 杂项 ────────────────────────────────────────────────────  │
│  深色模式         [═══════○══════]  开                        │
│  完成后是否提示   [○═══════════]  关                          │
│  下载弹窗方式     [浏览器窗口 ▼]                               │
│                                                                 │
│  [ 💾 保存 ]  [ 🔄 恢复默认 ]                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.5.2 详细规则

| 编号     | 规则描述                                                     | 优先级 |
| -------- | ------------------------------------------------------------ | ------ |
| **S-01** | 设置页面为独立 HTML 页面，通过扩展图标面板中的“设置按钮”跳转打开（新标签页） | P0     |
| **S-02** | **下载器1 配置区**：<br>① **名称**：文本输入框，自定义下载窗口按钮文字，默认“下载器1”<br>② **地址**：协议下拉框（HTTP/HTTPS/WebSocket/WebSocket Secure）+ IP 输入框 + 端口输入框 + 路径输入框（默认 `jsonrpc`）<br>③ **密钥**：可选文本输入框，用于 RPC 认证 | P0     |
| **S-03** | **下载器2 配置区**：同下载器1，默认名称“下载器2”             | P0     |
| **S-04** | **杂项配置区**：<br>① **深色模式开关**：Toggle 开关，控制全 UI 深色主题，**默认值 = 开（深色）**<br>② **完成后是否提示**：Toggle 开关，控制 Aria2 完成后是否发 Windows 通知，默认值 = 关<br>③ **下载弹窗方式**：下拉选择框，选项为“浏览器窗口”（默认）和“在页面右上角覆盖弹出” | P0     |
| **S-05** | **保存按钮**：点击后保存所有配置，显示“保存成功”或具体错误提示（如校验失败） | P0     |
| **S-06** | **恢复默认按钮**：一键还原所有配置为出厂默认值（深色=开，完成提示=关，弹窗方式=浏览器窗口，下载器名称/地址=默认） | P1     |
| **S-07** | **输入校验**：<br>① IP/域名格式校验（IPv4/IPv6/域名）<br>② 端口范围校验（1-65535）<br>③ 路径校验（禁止 `../`、`\` 等特殊字符）<br>校验失败时保存按钮置灰或提示具体错误字段 | P1     |

---

### 4.6 深色模式规则

| 编号        | 规则描述                                                     | 优先级 |
| ----------- | ------------------------------------------------------------ | ------ |
| **DARK-01** | 深色模式通过设置页“深色模式开关”统一控制，影响范围：下载弹窗、扩展图标面板、设置页面自身 | P0     |
| **DARK-02** | **默认状态为“开启”（深色）**，不跟随 Firefox 或系统主题变化，必须由用户手动切换 | P0     |
| **DARK-03** | 深色模式开关状态持久化保存                                   | P0     |

---

### 4.7 下载完成通知规则

| 编号     | 规则描述                                                     | 优先级 |
| -------- | ------------------------------------------------------------ | ------ |
| **N-01** | “完成后是否提示”开关 **开启** 时，Aria2 任务状态变为 `complete` 后，**通过浏览器 Notifications API 发送 Windows 系统通知** | P1     |
| **N-02** | 通知内容包含文件名                                           | P1     |
| **N-03** | “保存”（浏览器下载）方式 **不触发** 扩展的完成通知，Firefox 自带下载完成提示 | P1     |
| **N-04** | 通知点击后无额外操作（不打开文件夹，因为扩展无法获知 Aria2 服务端的 `dir` 配置） | P2     |

---

## 五、验收标准

### 5.1 下载截获与弹窗

| 编号      | 验收项                                              | 预期结果                                                     | 优先级 |
| --------- | --------------------------------------------------- | ------------------------------------------------------------ | ------ |
| **AC-01** | 全局开关开启 + 点击 HTTP/HTTPS 下载链接             | 弹出下载方式选择弹窗，不弹出 Firefox 原生下载窗口            | P0     |
| **AC-02** | 全局开关开启 + 点击 `blob:` / `data:` / `ftp:` 链接 | 不弹出扩展弹窗，直接使用 Firefox 默认下载                    | P0     |
| **AC-03** | 全局开关关闭 + 点击任意下载链接                     | 不弹出扩展弹窗，直接使用 Firefox 默认下载                    | P0     |
| **AC-04** | 弹窗展示文件名                                      | 文件名可编辑，修改后生效                                     | P0     |
| **AC-05** | 弹窗展示下载链接                                    | URL 完整（或截断 + hover 完整 + 复制按钮）                   | P1     |
| **AC-06** | 点击“下载器1”按钮                                   | 弹窗关闭 → 调用 Aria2 RPC `aria2.addUri`，透传 referrer/cookies/UA | P0     |
| **AC-07** | 点击“下载器2”按钮                                   | 同上，使用下载器2配置                                        | P0     |
| **AC-08** | 点击“保存”按钮                                      | 弹窗关闭 → 使用 Firefox 内置下载器                           | P0     |
| **AC-09** | 下载器未配置完整                                    | 对应按钮置灰禁用，hover 提示“请先在设置中配置”               | P0     |
| **AC-10** | 关闭弹窗（× / ESC / 点击外部）                      | 弹窗关闭，不执行任何下载操作（取消下载）                     | P0     |

### 5.2 RPC 调用与错误处理

| 编号      | 验收项                                 | 预期结果                                                     | 优先级 |
| --------- | -------------------------------------- | ------------------------------------------------------------ | ------ |
| **AC-11** | Aria2 RPC 调用超时/服务不可达/返回错误 | 弹窗提示“下载失败”，错误详情默认折叠，不重试，取消下载       | P0     |
| **AC-12** | RPC 调用参数验证                       | 扩展仅传递：`url`、`out`、`referer`、`cookie`、`user-agent`，不传递 `dir` 等覆盖性参数 | P0     |
| **AC-13** | 跨页面 Cookies 透传                    | 在页面 A 触发下载 → Aria2 收到的 Cookie 为页面 A 的 Cookie   | P0     |

### 5.3 扩展图标面板

| 编号      | 验收项       | 预期结果                             | 优先级 |
| --------- | ------------ | ------------------------------------ | ------ |
| **AC-14** | 点击扩展图标 | 弹出浮动面板，显示全局开关和设置按钮 | P0     |
| **AC-15** | 全局开关切换 | 状态持久化，刷新页面后仍保持         | P0     |
| **AC-16** | 点击设置按钮 | 新标签页打开设置页面                 | P0     |

### 5.4 设置页面

| 编号      | 验收项                             | 预期结果                                | 优先级 |
| --------- | ---------------------------------- | --------------------------------------- | ------ |
| **AC-17** | 修改下载器名称                     | 下载弹窗中对应按钮文字更新              | P0     |
| **AC-18** | 配置 RPC 地址（协议/IP/端口/路径） | 配置保存成功，下载器调用时使用该地址    | P0     |
| **AC-19** | 配置下载器密钥                     | 密钥保存成功，RPC 调用时携带 token 认证 | P0     |
| **AC-20** | 输入校验（IP/端口/路径）           | 非法输入时保存失败并提示具体错误字段    | P1     |
| **AC-21** | 点击“保存”按钮                     | 显示“保存成功”提示，配置持久化          | P0     |
| **AC-22** | 点击“恢复默认”按钮                 | 所有配置恢复为出厂默认值                | P1     |

### 5.5 深色模式

| 编号      | 验收项             | 预期结果                                       | 优先级 |
| --------- | ------------------ | ---------------------------------------------- | ------ |
| **AC-23** | 首次安装后深色模式 | 弹窗、设置页面、图标面板均为深色主题（默认开） | P0     |
| **AC-24** | 深色模式开关切换   | 全 UI 立即切换，状态持久化                     | P0     |

### 5.6 通知与弹窗方式

| 编号      | 验收项                        | 预期结果                                         | 优先级 |
| --------- | ----------------------------- | ------------------------------------------------ | ------ |
| **AC-25** | 完成提示开启 + Aria2 下载完成 | 弹出 Windows 系统通知，显示文件名                | P1     |
| **AC-26** | 完成提示关闭 + Aria2 下载完成 | 不弹出任何通知                                   | P1     |
| **AC-27** | “保存”方式下载完成            | 不弹出扩展通知                                   | P1     |
| **AC-28** | 弹窗方式 = 浏览器窗口（默认） | 点击下载 → 新开独立浏览器弹窗（500px，无地址栏） | P0     |
| **AC-29** | 弹窗方式 = 页面内覆盖弹出     | 点击下载 → 当前页面右上角浮动层显示弹窗内容      | P0     |

---

## 六、技术约束与边界

### 6.1 技术依赖

| 依赖项                 | 说明                                                         |
| ---------------------- | ------------------------------------------------------------ |
| Firefox Extensions API | 使用 `chrome.downloads.onCreated` 或 `webRequest.onBeforeRequest` 拦截下载 |
| Aria2 JSON-RPC         | 通过配置的 RPC 地址调用 `aria2.addUri` 方法                  |
| Notifications API      | 用于 Windows 系统通知（需申请 `notifications` 权限）         |
| Storage API            | 用于配置持久化保存（`chrome.storage.local`）                 |

### 6.2 权限申请

| 权限                                | 用途                                   |
| ----------------------------------- | -------------------------------------- |
| `downloads`                         | 拦截和接管下载请求                     |
| `webRequest` / `webRequestBlocking` | 获取请求 Headers 信息                  |
| `cookies`                           | 获取当前页面的 Cookie                  |
| `storage`                           | 保存扩展配置                           |
| `notifications`                     | 发送 Windows 下载完成通知              |
| `activeTab`                         | 获取当前活动页面的 URL（作为 Referer） |

### 6.3 边界与限制

| 边界项           | 说明                                                         |
| ---------------- | ------------------------------------------------------------ |
| 协议支持         | 仅拦截 HTTP/HTTPS，其他协议（blob/data/ftp 等）直接放行给浏览器 |
| 文件大小         | 无限制，由 Aria2 服务端配置决定                              |
| 并发下载数       | 无限制，由 Aria2 服务端配置决定                              |
| 下载目录         | 由 Aria2 服务端 `dir` 配置决定，扩展不传递                   |
| 多标签页 Cookies | 每个下载使用触发时所在标签页的 Cookies                       |

---

## 七、附录

### 7.1 典型用户操作路径

**路径一：使用 Aria2 下载（首次需配置）**

```
用户安装扩展 → 点击工具栏图标 → 点击“设置” → 配置下载器1 RPC 地址 → 保存
→ 点击下载链接 → 弹窗出现 → 点击“下载器1” → Aria2 开始下载 → 完成后收到 Windows 通知
```

**路径二：使用浏览器下载**

```
用户点击下载链接 → 弹窗出现 → 点击“保存” → Firefox 原生下载
```

**路径三：临时关闭扩展**

```
用户点击工具栏图标 → 关闭全局开关 → 所有下载走 Firefox 默认行为
```

**路径四：取消下载**

```
用户点击下载链接 → 弹窗出现 → 点击 × / ESC / 点击外部 → 弹窗关闭，无事发生
```

---

### 7.2 默认配置一览表

| 配置项         | 默认值                          |
| -------------- | ------------------------------- |
| 全局开关       | 开（启用截获）                  |
| 下载器1 名称   | "下载器1"                       |
| 下载器1 地址   | `ws://127.0.0.1:16800/jsonrpc`  |
| 下载器1 密钥   | 空                              |
| 下载器2 名称   | "下载器2"                       |
| 下载器2 地址   | `http://127.0.0.1:6800/jsonrpc` |
| 下载器2 密钥   | 空                              |
| 深色模式       | 开（深色）                      |
| 完成后是否提示 | 关                              |
| 下载弹窗方式   | 浏览器窗口                      |
